<div>
  <%= image_tag @book.get_image %>
  <p>本のタイトル：<%= @book.title %></p>
  <p>投稿ユーザー画像：<%= image_tag 'sample-author1.jpg' %>
  <p>感想：<%= @book.user.name %></p>
  <p>投稿日：<%= @book.created_at.strftime('%Y/%m/%d') %></p>
</div>